import BookingsList from './BookingsList';

export const metadata = {
  title: 'Bookings | Connectly - Service Marketplace',
  description: 'View and manage your service bookings.',
};

export default function BookingsPage() {
  return <BookingsList />;
}
